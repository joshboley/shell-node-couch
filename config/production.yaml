# Place any production configurations here.
# If you need to add additional environments, just
# add a new yaml file to the config directory and 
# pass the name of the file (no file extension) to the 
# NODE_ENV=<configuration> variable on server startup

port: 8000
couchbase:
  connectionString: 'couchbase://place-id/dns-here'
  bucket: 'place bucket name here'